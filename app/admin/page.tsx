export const dynamic = 'force-dynamic';

export default function AdminPage() {
  return (
    <section>
      <h1>Admin Panel</h1>
      <p>RBAC kontrolü middleware üzerinden yapılır.</p>
    </section>
  );
}
